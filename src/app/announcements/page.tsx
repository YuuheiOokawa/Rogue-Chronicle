import { AnnouncementsScreen } from '@/features/announcements/announcements-screen';

/** SCR-115 お知らせ画面（docs/09 §5.2。API-104は認証不要のため未ログインでも閲覧可） */
export default function AnnouncementsPage() {
  return <AnnouncementsScreen />;
}
